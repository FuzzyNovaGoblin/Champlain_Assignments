#include <vector>
#include <iostream>

using namespace std;

double sum(vector<string>& arr);
double avg(vector<string>& arr);
double lowest(vector<string>& arr);

void camelCase(string &str);

char tooUpper(char c);